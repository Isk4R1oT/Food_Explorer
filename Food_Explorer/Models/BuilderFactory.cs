/*using Food_Explorer.Entities;

namespace Food_Explorer.Models
{
    public interface IBuilder
    {
              
    }
    public enum BuilderType
    {
        UserBuilder,
        ProductBuilder,
        OrderBuilder,
    }
    public class BuilderFactory : IBuilder
    {      
        public void Init(BuilderType builderType)
        {
            IBuilder builder;
            switch(builderType)
            {
                case BuilderType.UserBuilder:
                    builder = new UserBuilder();
                    break;
                case BuilderType.ProductBuilder:
                    builder = new ProductBuilder();
                    break;
                case BuilderType.OrderBuilder:
                    builder = new OrderBuilder();
                    break;
            }
        }
    }
}
*/

//de42e3eq3qe
